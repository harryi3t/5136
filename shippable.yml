# language setting
language: python

# Version number
python:
  - 2.7

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO

build:
  ci:
    - echo "Hello, World!"
    - sleep 10m
    
